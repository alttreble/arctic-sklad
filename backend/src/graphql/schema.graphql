type Item {
  id: Int!
  createdAt: String!
  updatedAt: String!
  name: String!
  description: String
  expirationDate: String
}

type Query {
    items: [Item]!
}

type Mutation {
  addItem: Item
}