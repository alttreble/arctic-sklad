query item($itemId: Int!) {
  item(id: $itemId) {
    id
    createdAt
    updatedAt
    name
    genericName
    totalQuantity
    hasExpiredEntry
    entries {
      createdAt
      updatedAt
      expirationDate
      hasExpired
      quantity
    }
    uom {
      name
      id
    }
  }
}